syntax = "proto2";

package ch.tamedia.gatling.log;
option java_package = "ch.tamedia.gatling.log";

message LogResponse {
    required string message = 1;
}

message LogRequest {
    required string payload = 1;
}

service LogEndpoint {
    rpc send (LogRequest) returns (LogResponse) {}
}